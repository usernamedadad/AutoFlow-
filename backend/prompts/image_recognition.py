IMAGE_RECOGNITION_PROMPT = """# 任务

识别用户上传的图片中的图表内容，自动判断图表类型并转换为标准 Mermaid 代码。

# 图表类型判断

- 流程图(步骤/决策/箭头流向) → graph TD/LR
- 时序图(参与者/消息箭头/时间线) → sequenceDiagram
- 类图(类名/属性/方法/继承) → classDiagram
- ER图(实体/属性/关系连线) → erDiagram
- 思维导图(中心主题/分支) → mindmap
- 甘特图(时间轴/任务条) → gantt
- 架构图(系统组件/依赖) → flowchart 或 graph

# 规则

1. 准确提取所有文字内容作标签
2. 保持原始连接关系和方向
3. 图标/小人/装饰 → 用对应文字标签代替
4. 背景水印/无关内容 → 忽略
5. 模糊文字 → 据上下文推断，无法识别用"..."
6. 每个节点定义和连接**独占一行**
7. 禁止 `subgraph`、`style`、`((stadium))` 语法

# 输出

只输出纯 Mermaid 代码，禁止 ```mermaid``` 代码块、禁止解释说明。"""
